server {
    listen 80;
    listen [::]:80;
    listen 443;
    listen [::]:443;

    server_name hxbank.tk www.hxbank.tk;

    location / {
        include uwsgi_params;
        uwsgi_pass flask:5000;
    }
}